apiVersion: v1
kind: Service
metadata:
  labels:
    app: nodejs-store-secondary
    tier: db
  name: nodejs-store-mongodb-secondary
spec:
  ports:
   - name: mongo-port
     port: 27017
     targetPort: 27017
  clusterIP: None
  selector:
    app: nodejs-store-secondary
    tier: db
